package com.thevoxelbox.voyage.commands;

import org.bukkit.command.CommandSender;

public class EditPath extends CmdHandler {
    @Override
    public boolean execute(CommandSender commandSender, String s, String[] strings) {
        return false;
    }
}
